# jekyll serve -w

name: CARB.IO
description: "A fast, low-latency, low-memory footprint, web JSON-API framework."

url: http://carb.io

markdown: redcarpet
pygments: true
cache: false

assets:
  compress:
    css: sass
    js: uglifier
